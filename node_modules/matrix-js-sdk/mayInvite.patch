diff --git a/lib/models/room-state.js b/lib/models/room-state.js
index 6f74a15..0b6fe0d 100644
--- a/lib/models/room-state.js
+++ b/lib/models/room-state.js
@@ -299,6 +299,18 @@ RoomState.prototype.maySendStateEvent = function(stateEventType, userId) {
 };
 
 /**
+ * Returns true if the given user ID has permission to send a regular
+ * invite to roomId (not a third party invite)
+ * @param {string} userId The ID of the would-be inviter
+ * @param {string} roomId The ID of the room to which the invitation would be issued
+ * @return {boolean} true if the given user ID should be permitted to issue
+                          a regular invite for the given room
+ *                        according to the room's state.
+ */
+RoomState.prototype.mayInviteToRoom = function(userId, roomId) {
+}
+
+/**
  * The RoomState class.
  */
 module.exports = RoomState;
