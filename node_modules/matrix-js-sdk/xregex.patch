diff --git a/lib/pushprocessor.js b/lib/pushprocessor.js
index 95a90a9..5cf3dcc 100644
--- a/lib/pushprocessor.js
+++ b/lib/pushprocessor.js
@@ -17,6 +17,11 @@ limitations under the License.
  * @module pushprocessor
  */
 
+var XRegExp = require('xregexp');
+
+// auto-adds itself to the XRegExp var
+require('../node_modules/xregexp/src/addons/unicode-base.js');
+
 /**
  * Construct a Push Processor.
  * @constructor
@@ -160,8 +165,8 @@ function PushProcessor(client) {
 
         var displayName = room.currentState.getMember(client.credentials.userId).name;
 
-        var pat = new RegExp("\\b" + escapeRegExp(displayName) + "\\b", 'i');
-        return ev.content.body.search(pat) > -1;
+        return XRegExp.test(ev.content.body,
+                            XRegExp("\\b" + XRegExp.escape(displayName) + "\\b", "iA"));
     };
 
     var eventFulfillsDeviceCondition = function(cond, ev) {
diff --git a/package.json b/package.json
index c0928fd..a23370d 100644
--- a/package.json
+++ b/package.json
@@ -26,7 +26,8 @@
     "browser-request": "^0.3.3",
     "browserify": "^10.2.3",
     "q": "^1.4.1",
-    "request": "^2.53.0"
+    "request": "^2.53.0",
+    "xregexp": "^3.1.0"
   },
   "devDependencies": {
     "watchify": "^3.2.1",
