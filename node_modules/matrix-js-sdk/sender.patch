diff --git a/spec/test-utils.js b/spec/test-utils.js
index 3955918..cc1961d 100644
--- a/spec/test-utils.js
+++ b/spec/test-utils.js
@@ -49,6 +49,7 @@ module.exports.mock = function(constr, name) {
  * @param {string} opts.type The event.type
  * @param {string} opts.room The event.room_id
  * @param {string} opts.user The event.user_id
+ * @param {string} opts.sender The sender user ID for the event. Default: opts.user
  * @param {string} opts.skey Optional. The state key (auto inserts empty string)
  * @param {Object} opts.content The event.content
  * @param {boolean} opts.event True to make a MatrixEvent.
@@ -61,7 +62,7 @@ module.exports.mkEvent = function(opts) {
     var event = {
         type: opts.type,
         room_id: opts.room,
-        sender: opts.user,
+        sender: opts.sender || opts.user,
         content: opts.content,
         event_id: "$" + Math.random() + "-" + Math.random()
     };
@@ -105,6 +106,7 @@ module.exports.mkPresence = function(opts) {
  * @param {string} opts.room The room ID for the event.
  * @param {string} opts.mship The content.membership for the event.
  * @param {string} opts.user The user ID for the event.
+ * @param {string} opts.sender The sender user ID for the event. Default: opts.user
  * @param {string} opts.skey The other user ID for the event if applicable
  * e.g. for invites/bans.
  * @param {string} opts.name The content.displayname for the event.
